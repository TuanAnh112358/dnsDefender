# Core dependencies
python-dateutil>=2.8.2

# Machine Learning (optional but recommended)
scikit-learn>=1.0.0
numpy>=1.21.0

# Web dashboard (optional)
Flask>=2.0.0
Jinja2>=3.0.0

# Email support
# Note: smtplib is built into Python

# System utilities
# Note: subprocess, socket, etc. are built into Python

# Data processing
# Note: json, csv, etc. are built into Python

# Development and testing (optional)
pytest>=6.0.0
pytest-cov>=3.0.0
black>=22.0.0
flake8>=4.0.0

# Documentation (optional)
sphinx>=4.0.0
sphinx-rtd-theme>=1.0.0